const gameOptionsElement = document.getElementById('game-options');
const gameResultElement = document.getElementById('game-result');
const playAgainElement = document.getElementById('play-again');

const playerScoreOutput = document.getElementById('player-score');
const pcScoreOutput = document.getElementById('pc-score');
const playerSelectionOutput = document.getElementById('player-selection');
const pcSelectionOutput = document.getElementById('pc-selection');
const resultOutput = document.getElementById('result-output');

export {
	gameOptionsElement,
	gameResultElement,
	playAgainElement,
	playerScoreOutput,
	pcScoreOutput,
	playerSelectionOutput,
	pcSelectionOutput,
	resultOutput
};
